"""Random-forest backend. Everything RF-specific — the training backends
(sklearn CPU, cuML GPU, LightGBM GPU), the training/prediction dispatch,
and the pipeline train/evaluate steps — lives in this package."""
